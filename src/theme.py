from matplotlib.colors import LinearSegmentedColormap

# ==========================================
# 🌸 Sakura & Lime Theme (樱花 & 青柠 - 高亮版)
# ==========================================

# 1. 基础单色 (用于线条、散点、文字)
# ------------------------------------------
# 绿色端 (负相关/保护): 鲜亮的青柠色
COLOR_PROTECTIVE = "#8FD94D" 

# 粉色端 (正相关/风险): 明亮海棠粉
COLOR_RISK = "#FF5588"

# 2. 渐变色板 (用于热力图 Heatmap)
# ------------------------------------------
# 定义渐变节点：绿 -> 浅绿 -> 白 -> 浅粉 -> 红
_colors_list = [
    COLOR_PROTECTIVE,   # [最负] -1.0
    "#D9F0C0",          # [微负] 过渡浅绿
    "#FFFFFF",          # [中点] 纯白 (0.0)
    "#FFC4D6",          # [微正] 过渡浅粉
    COLOR_RISK          # [最正] +1.0
]

# 创建 Matplotlib 色板对象
cmap_sakura_lime = LinearSegmentedColormap.from_list("SakuraBright", _colors_list, N=256)

# 3. Plotly 专用色板 (如果你以后要用 Plotly 画热力图)
# ------------------------------------------
# Plotly 需要 (数值, 颜色) 的元组列表
plotly_scale_sakura = [
    [0.0, COLOR_PROTECTIVE],
    [0.5, "#FFFFFF"],
    [1.0, COLOR_RISK]
]
